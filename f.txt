MMM modification_description MMM
Refactor replace_block and associated helpers for clarity and simplicity—flatten control flow, isolate lexical matching, and perform duplicate removal + insertion in a single structured step. This preserves behavior but makes reasoning straightforward and minimizes redundant traversal.

MMM update_declaration MMM
pyvibepatcher.py.replace_block
@@@@@@
def replace_block(content: str,
new_code: str,
target_name: Optional[str] = None,
kind: Optional[str] = None,
lexical_chain: Optional[List[str]] = None) -> tuple[str, bool]:
"""
Replace or delete a function, class, or assignment using LibCST in a clean, deterministic manner.